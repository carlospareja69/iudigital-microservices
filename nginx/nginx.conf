events {}

http {

    upstream project_service {
        server project_service_1:3000;
        server project_service_2:3000;
        server project_service_3:3000;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://project_service;
        }
    }
}